A little asteroid game I made to learn SFML

![](/example.png)

## Installation (Linux)
Clone the repo wherever and then in the repository root:
```bash
cmake -B build
cmake --build build -j4
```
You will then find the binary in the build directory:
```bash
./build/bin/main
```

## Controls
Mouse to target and fire, WASD to accelerate according to orientation
